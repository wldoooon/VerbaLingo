# Don't send these to Docker — they're heavy and unnecessary
node_modules
.next
.git
.gitignore
*.md

# Environment files (secrets — never bake into an image)
.env*
!.env.example

# IDE
.vscode
.idea

# Testing
coverage
cypress
__tests__
.eslintcache

# Docker meta (don't recursively include ourselves)
Dockerfile*
docker-compose*
.dockerignore

# TypeScript build info (not needed for Docker build)
tsconfig.tsbuildinfo
next-env.d.ts
