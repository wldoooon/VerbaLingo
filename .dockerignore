# ═══════════════════════════════════════════
# Root .dockerignore — Backend builds only
# ═══════════════════════════════════════════
# Only the FastAPI backend uses Docker now.
# Frontend (Next.js) is deployed to Vercel separately.
#
# This file excludes everything the backend doesn't need,
# keeping the build context small and fast (~1MB instead of 800MB+).

# ─── Version control ───
.git
.gitignore
.gitattributes
.github

# ─── Python artifacts ───
__pycache__
*.pyc
*.pyo
.pytest_cache
venv
.venv

# ─── Frontend (deployed to Vercel, not needed here) ───
ui

# ─── Environment files (contain secrets!) ───
.env
.env.*
!.env.example

# ─── Heavy data files ───
dataset
dataset.rar
scraping_test
logs

# ─── IDE / editor config ───
.vscode
.idea

# ─── Documentation & notes ───
*.md
notes

# ─── Docker meta (don't include in build context) ───
docker-compose*
.dockerignore

# ─── Other project dirs not needed for backend ───
extension
verbaLingo_agent
.taskmaster
.kilocode
.playwright-mcp
.agent
manticore
